# AGILE: Hand-Object Interaction Reconstruction from Video via Agentic Generation

**arXiv**: 2602.04672 | **주제 분류**: Hand-Object Interaction | **출판일**: 2026-02-04 | **학회**: SIGGRAPH 2026 (Conference Papers)
**저자/소속**: Jin-Chuan Shi, Binhong Ye, Tao Liu, Xiaoyang Liu, Yangjinhui Xu, Junzhe He, Zeju Li, Hao Chen, Chunhua Shen (State Key Lab of CAD & CG, Zhejiang University / Zhejiang University of Technology)
**링크**: https://arxiv.org/abs/2602.04672

## 한 줄 요약
단안(monocular) 영상에서 손-물체 상호작용을 복원할 때, 가려진 부분을 "재구성"으로 메우는 대신 VLM이 감독하는 생성(agentic generation)으로 완결된 물체 메시를 먼저 만들고, 접촉이 시작되는 한 프레임만 앵커로 잡아 추적하는 방식으로 SfM 의존성을 없앤 프레임워크다.

## 메인 그림
![AGILE: 단안 영상에서 가위·펜 같은 얇은 물체부터 게임 컨트롤러 같은 복잡한 위상까지, 심한 손 가림 아래에서도 watertight 지오메트리와 6D 포즈를 복원한 결과](https://arxiv.org/html/2602.04672v3/x1.png)

## 선행 연구
손-물체 상호작용(HOI, hand-object interaction) 복원 연구는 크게 세 갈래로 흘러왔다.

첫째, **3D 손 복원**은 MANO 같은 파라메트릭 모델을 이미지에 맞추는 방식에서 출발해 영상에서 4D 궤적을 회귀하는 쪽으로 발전했다. 다만 시간축을 다루는 최신 방법들은 접촉 분석에 꼭 필요한 프레임 단위 기하 정밀도를 희생하는 경향이 있어서, 이 논문은 WiLoR 같은 프레임 단위 추정기를 초기 손 메시로 쓴다.

둘째, **생성 기반 3D 물체 복원**은 NeRF나 3D Gaussian Splatting처럼 조밀한 다시점 관측을 요구하는 최적화에서, 희소한 입력만으로도 에셋을 만들어내는 LRM·확산 모델 계열로 옮겨가고 있다. 하지만 [SAM 3D](https://arxiv.org/abs/2511.16624) 같은 단일 시점 방법은 영상 전체의 다시점 단서를 모으지 못해 심한 가림을 풀지 못한다.

셋째, **HOI 복원** 자체는 초기의 템플릿 기반·깊이 의존 방법에서 HOLD, BIGS 같은 암시적(implicit) 표현 최적화로 넘어왔고, MagicHOI처럼 확산 사전지식(diffusion prior)을 끌어들이는 시도도 나왔다.

## 문제 제기
논문은 기존 파이프라인에 두 개의 결정적 장벽이 있다고 본다.

**첫째, 재구성 기반 지오메트리의 한계.** 뉴럴 렌더링은 근본적으로 다시점 일관성을 전제로 하는데, HOI 상황에서는 손이 물체를 심하게 가리기 때문에 이 전제가 깨진다. 결과물은 조각나거나(fragmented) 노이즈가 끼거나 겉면이 닫히지 않은(non-watertight) 메시가 되고, 학습된 시점에서 보기엔 그럴듯해도 깨끗하고 명시적인 위상(topology)을 요구하는 물리 엔진에는 쓸 수 없다.

**둘째, 포즈 초기화의 취약성.** 대부분의 방법이 COLMAP 같은 SfM(Structure-from-Motion, 여러 시점 이미지에서 카메라·구조를 동시에 복원하는 고전 기법)에 기대는데, SfM은 질감 없는 물체, 빠른 움직임, 심한 가림이 있는 동적 장면에서 프레임 정합에 실패하기 쉽다. 한 번 실패하면 파이프라인 전체가 무너진다. 실제로 논문 실험에서 MagicHOI는 DexYCB 시퀀스의 75%에서, HOLD는 55%에서 실패했다.

생성 모델을 쓰면 될 것 같지만 여기에도 함정이 있다. 표준 3D 생성기는 동적 영상에는 존재하지 않는 정규(canonical) 카메라를 요구하므로 2D 생성 사전지식에 의존해야 하는데, 2D 확산 모델은 확률적이라 영상 속 실제 물체와 어긋나는 환각(hallucination)을 자주 만든다. 결국 **어떤 프레임이 정보량이 많은지 고르고, 생성된 시점을 일관성 기준으로 엄격히 걸러내는 지능적 장치**가 필요한데, 기존 end-to-end 파이프라인에는 이 기능이 없다.

## 연구 주제
이 논문은 상호작용 학습의 패러다임을 **재구성(reconstruction)에서 에이전트형 생성(agentic generation)으로 전환**한다. 목표는 단순히 보기 좋은 복원이 아니라, 물리 시뮬레이터에 그대로 올릴 수 있는 **simulation-ready 디지털 트윈**을 임의의 단안 영상에서 자동으로 뽑아내는 것이다. 이를 위해 (1) VLM이 품질을 심사하는 물체 에셋 생성, (2) SfM을 완전히 걷어낸 anchor-and-track 초기화·추적, (3) 물리적 타당성을 강제하는 접촉 인식(contact-aware) 최적화라는 세 축을 제안한다.

## 연구 방법
**입력/출력**: 고정 카메라로 찍은 손-물체 상호작용 단안 영상 → 손과 물체의 4D 궤적 + 텍스처가 입혀진 watertight 물체 메시.

### 1) Agentic Textured Object Generation (§3.1)
- **VLM 기반 키프레임 선택**: VLM 에이전트(Gemini 3 Pro)가 영상에서 시점 커버리지를 최대화하는 정보량 높은 키프레임 $N$개(보통 1~4장)를 고른다. 한 장만 쓰는 단일 시점 방식과 갈라지는 지점이다.
- **다시점 합성 + VLM 비평가(critic)**: 선택된 프레임으로 이미지 생성 모델(Gemini 2.5 Flash)이 물체의 직교 시점들을 합성한다. 그다음 VLM 비평가가 생성된 뷰와 원본 영상 프레임의 일관성을 기하·텍스처·재질 관점에서 점수 매기고, 기준 미달 후보는 버리고 다시 생성한다(rejection sampling).
- **3D 리프팅과 메시 정제**: 검증된 다시점 이미지를 feed-forward 3D 생성 모델([Hunyuan3D](https://arxiv.org/abs/2501.12202))에 넣어 초기 메시를 얻고, Blender의 quad-remesh로 약 10,000 face 수준의 자동 리토폴로지와 Smart UV 언랩을 수행해 시뮬레이션에 적합한 깨끗한 메시를 만든다.
- **텍스처 정제도 에이전트가 감독**: image-to-image 편집 모델로 고해상도 다시점을 조건 삼아 디테일을 살리고, 이 결과도 VLM 비평가가 rejection sampling으로 재검증한다. 텍스처 품질이 중요한 이유는 미학이 아니라 **하류 작업 때문**이다. FoundationPose는 선명한 표면 특징에 의존해 매칭하고, 뒤이은 최적화는 DINO 특징을 쓰기 때문이다.

### 2) SfM-Free 초기화 (§3.2)
- **전처리**: MoGe-2로 카메라 내부 파라미터 $\mathbf{K}$와 프레임별 metric depth $\mathbf{D}$를, [SAM2](https://arxiv.org/abs/2408.00714)로 손·물체 마스크 $\mathbf{M}_h, \mathbf{M}_o$를 뽑는다. 카메라 좌표계를 그대로 월드 좌표계로 쓴다.
- **손 초기화**: WiLoR로 MANO 파라미터($\boldsymbol{\beta}, \boldsymbol{\theta}, \mathbf{R}_h$)와 2D 키포인트 $\mathbf{J}_{2D} \in \mathbb{R}^{21\times2}$를 예측한다. 회전은 신뢰할 만하므로 고정하고, 깊이 포인트클라우드에 제한된 ICP를 걸어 전역 스케일 $s_h$를 구한 뒤, PnP로 프레임별 translation $\mathbf{T}_h$를 푼다.
- **물체 스케일과 앵커 포즈**: 같은 ICP로 물체 전역 스케일 $s_o$를 먼저 정하고(scale-first), **Interaction Onset Frame(IOF, 상호작용이 시작되는 프레임)** 을 물체 마스크의 유의미한 변위로 찾은 뒤, 그 한 프레임에서만 FoundationPose로 초기 6D 포즈 $[\mathbf{R}_o^{IOF}, \mathbf{T}_o^{IOF}]$를 잡는다. IOF는 가시 물체 픽셀 변화율 $r_i$가 임계값 $\tau=0.025$를 넘고 물체 마스크가 이미지 경계에 닿지 않는 첫 프레임으로 정의한다.

왜 하필 IOF에서 시작하는가? 두 가지 이유다. 첫째, 단안 복원의 스케일 모호성을 손의 믿을 만한 metric scale로 붙잡아 물체 크기를 전파할 수 있고, 둘째, 접촉 기반 손실은 실제로 접촉이 일어날 때만 물리적으로 의미 있는 gradient를 준다.

### 3) Contact-Aware Optimization (§3.3)
IOF를 기점으로 영상의 앞뒤 양방향(bi-directional)으로 프레임 단위 온라인 최적화를 전파한다. IOF에서만 물체의 비등방(anisotropic) 스케일 $\mathbf{s}_o$를 함께 최적화하고, 이후 프레임에서는 동결한다. 각 프레임은 2단계다.

**Step 1 — 손 translation 정제**: $\mathbf{R}_h$와 $s_h$를 고정하고 $\mathbf{T}_h$만 최적화한다. 관절 재투영 손실 $\mathcal{L}_{\text{joint}} = \frac{1}{N_j}\sum_{i=1}^{N_j}\|\Pi(\mathbf{J}_{3D}^{(i)}) - \mathbf{J}_{2D}^{(i)}\|_2^2$ 하나만 쓴다. 순수 기하 갱신이라 손이 물체의 안정적인 앵커 역할을 한다.

**Step 2 — 상호작용 인식 물체 추적**: 손을 고정한 채 $(\mathbf{R}_o, \mathbf{T}_o)$를 최적화한다. 목적 함수는

$$\mathcal{L}_{\text{obj}} = \lambda_{\text{mask}}\mathcal{L}_{\text{mask}} + \lambda_{\text{dino}}\mathcal{L}_{\text{dino}} + \lambda_{\text{interact}}\mathcal{L}_{\text{interact}}$$

- $\mathcal{L}_{\text{mask}} = \|\mathbf{A}_{\text{ren}} - \mathbf{M}_{\text{gt}}\|_2^2$: 렌더링된 실루엣과 GT 마스크의 $L_2$ 거리로 픽셀 수준 경계를 맞춘다.
- $\mathcal{L}_{\text{dino}}$: [DINOv3](https://arxiv.org/abs/2508.10104) 특징으로 3D-2D 의미 대응을 만든다. 정규 물체 표면에서 $N_p$개 점을 샘플링해 유사도 맵 $\mathbf{S}_i$를 만들고, 현재 포즈로 투영한 좌표 $\mathbf{u}_i = \Pi(\mathbf{R}_o(\mathbf{s}_o \odot \mathbf{p}_i) + \mathbf{T}_o)$에서의 유사도를 가림 마스크 $\mathcal{M}_{occ}$로 가중해 최대화한다. 픽셀 단위 photometric loss와 달리 조명 변화에 강하다.
- $\mathcal{L}_{\text{interact}}$: 잡고 있는 동안 손과 물체의 상대 배치가 거의 안 변한다는 물리 사전지식이다. 손 정점을 물체 국소 좌표계로 옮겨 $\tilde{\mathbf{v}}_{h,i}^{(t)} = (\mathbf{R}_o^{(t)})^\top(\mathbf{v}_{h,i}^{(t)} - \mathbf{T}_o^{(t)})$, 이전 프레임 대비 변위를 최소화한다. 물체 SDF $\Phi(\cdot)$ 기반 가중치 $w_i = 1 - \tanh(\sigma \cdot d_i)$($\sigma=40$)로 실제 잡는 부위에만 제약이 걸리게 하고, 최대 거리 0.05m로 잘라 엉뚱한 장거리 인력을 막는다.

**구현**: 손실 가중치는 $\mathcal{L}_{\text{mask}}$ 5.0, $\mathcal{L}_{\text{dino}}$ 10.0, $\mathcal{L}_{\text{interact}}$는 HO3D 400 / DexYCB 200이다. 회전·이동에 별도 Adam을 쓰고 학습률은 물체 회전 $2\times10^{-3}$, 이동 $1\times10^{-3}$로 고정한다. 프레임당 손 translation 최대 200 iteration, 물체 6D 포즈 최대 400 iteration이다.

## 실험 결과 / 연구 의의
**데이터셋**: DexYCB(20개 궤적), HO3D-v3(18개 시퀀스, HOLD 프로토콜), ARCTIC(rigid-object subset, 양손 평가), 그리고 자체 수집을 포함한 in-the-wild 시퀀스. 시퀀스는 stride 5로 서브샘플링한다.

**지표**: 손은 root-relative MPJPE(mm), 물체 기하는 Chamfer Distance(CD, $cm^2$)와 F@5mm/F@10mm, 상호작용 품질은 손 root 좌표계로 정렬한 hand-relative CD($CD_h$), 그리고 초기화 붕괴나 파국적 드리프트를 실패로 세는 **Success Rate(SR)** 다.

### DexYCB / HO3D-v3 정량 비교 (†는 성공한 시퀀스만 평균 → survivor bias)

| 데이터셋 | 방법 | MPJPE(mm)↓ | CD($cm^2$)↓ | F@5(%)↑ | F@10(%)↑ | $CD_h$($cm^2$)↓ | SR(%)↑ |
|---|---|---|---|---|---|---|---|
| DexYCB | HOLD† | 30.86 | 19.30 | 33.20 | 54.94 | 170.9 | 45.0 |
| DexYCB | MagicHOI† | 21.20 | 2.05 | 45.67 | 67.14 | 661.90 | 25.0 |
| DexYCB | **AGILE** | **19.06** | **0.52** | **83.21** | **95.43** | **94.60** | **100.0** |
| HO3D-v3 | HOLD† | 22.09 | 1.11 | 81.75 | 92.42 | 18.66 | 100.0 |
| HO3D-v3 | MagicHOI† | 7.38 | 0.90 | 76.74 | 91.59 | 21.81 | 83.3 |
| HO3D-v3 | **AGILE** | **3.92** | **0.27** | **86.63** | **97.77** | **15.81** | **100.0** |

DexYCB에서 CD를 MagicHOI의 $2.05\ cm^2$ 대비 $0.52\ cm^2$로 약 75% 줄였다. 특히 눈여겨볼 점은 **survivor bias 해석**이다. 베이스라인 수치는 초기화에 성공한 쉬운 시퀀스만 평균 낸 값인데, AGILE은 가장 어려운 가림 케이스까지 전부 포함해 평균 내고도 MPJPE 19.06mm로 MagicHOI의 21.20mm(쉬운 부분집합 기준)보다 낮다. 즉 더 견고할 뿐 아니라 근본적으로 더 정확하다는 뜻이다.

상호작용 품질에서도 격차가 크다. MagicHOI의 $CD_h$ 661.90은 물체가 손에서 자주 떨어져 떠다닌다는 신호인데, AGILE은 94.60으로 물체를 손에 단단히 붙여 놓는다.

### ARCTIC 양손(bimanual) 비교

| 방법 | MPJPE$_l$↓ | MPJPE$_r$↓ | $CD_o$↓ | F@5↑ | $CD_l$↓ | $CD_r$↓ |
|---|---|---|---|---|---|---|
| HOLD | 27.1 | 24.7 | 2.07 | 37.1 | 105.9 | 123.5 |
| BIGS | 34.1 | 36.1 | 1.36 | 56.4 | 46.1 | 31.3 |
| **AGILE** | **25.0** | **23.8** | **1.12** | **57.6** | **21.9** | **30.6** |

구조를 바꾸지 않고 두 손을 각각 독립 접촉 제약으로 함께 최적화하는 것만으로 양손 상황에 확장된다.

### Ablation (HO3D)

| 변형 | MPJPE↓ | CD↓ | F@5mm↑ | $CD_h$↓ |
|---|---|---|---|---|
| (a) AGILE (Full) | 3.92 | 0.27 | 86.63 | 15.81 |
| (b) w/o Agentic Multi-view Gen. | 4.02 | 2.85 | 30.12 | 40.12 |
| (b₁) Heuristic Selection (VLM critic → max-mask) | 4.17 | 2.04 | 76.51 | 29.37 |
| (b₂) w/o Multi-View Synthesis | 4.32 | 2.61 | 66.58 | 46.94 |
| (c) w/o Texture Refine. | 4.16 | 0.30 | 86.02 | 20.18 |
| (d) w/o $\mathcal{L}_{\text{joint}}$ | 5.06 | 0.33 | 84.93 | 19.37 |
| (e) w/o $\mathcal{L}_{\text{mask}}$ | 3.97 | 0.40 | 81.14 | 23.99 |
| (f) w/o $\mathcal{L}_{\text{dino}}$ | 4.13 | 0.34 | 84.36 | 21.64 |
| (g) w/o $\mathcal{L}_{\text{interact}}$ | 4.17 | 0.35 | 86.10 | **54.40** |

읽어낼 지점이 몇 개 있다.
- 에이전트 루프를 통째로 빼면 CD가 0.27 → 2.85로 10배 넘게 나빠진다. VLM 비평가를 단순 "가장 큰 마스크" 휴리스틱으로 바꾸기만 해도(b₁) 크게 떨어지는데, 큰 마스크가 정보량 많은 시점이 아니라 자기 가림이 심한 시점인 경우가 많기 때문이다. 이는 성능 향상이 기반 파운데이션 모델의 능력이 아니라 **에이전트 설계 자체**에서 나온다는 근거다(두 변형 모두 동일한 Gemini/Hunyuan3D를 쓴다).
- 텍스처 정제를 빼면 CD는 거의 그대로인데 $CD_h$가 15.81 → 20.18로 오른다. 텍스처는 미관이 아니라 FoundationPose 초기화와 DINO 손실의 재료라는 논지를 뒷받침한다.
- $\mathcal{L}_{\text{interact}}$를 빼면 $CD_h$가 54.40으로 폭등한다. 표면 거리를 줄이려고 물체가 손 메시를 뚫고 들어가거나 부자연스럽게 미끄러진다.

### 계산 비용 (200프레임, RTX 4090 1장)

| 방법 | 전처리 | 최적화 | 총합 |
|---|---|---|---|
| HOLD | ~65분 | ~24시간 | ~25.1시간 |
| MagicHOI | ~25분 | ~2시간 | ~2.4시간 |
| **AGILE** | ~21–29분 | ~1.75시간 | **~2.1–2.2시간** |

VLM 비평가를 루프에 넣었는데도 총 시간은 베이스라인과 비슷하거나 더 빠르다. 전처리 중 에이전트 생성 단계가 ~16–24분(rejection sampling 반복 $n \in [1,3]$에 따라 다름)이고, 키프레임 선택 ~2분, 3D 생성 ~10분, 다시점 합성 ~2–6분, 텍스처 정제 ~2–6분으로 쪼개진다.

### Real-to-Sim 응용
복원한 시퀀스를 Isaac Gym에 올리고 사람 손 동작을 Shadow 같은 다지(dexterous) 로봇 손에 kinematic 최적화로 리타게팅했다. 생성된 메시가 watertight하고 위상이 깨끗해서 수작업 정리 없이 시뮬레이터에 바로 로드된다. 다만 논문 스스로 이것은 물리 기반 rollout이 아니라 **kinematic 시각화**임을 분명히 한다.

**의의**: 인터넷의 무제한 상호작용 영상을 로봇 조작 학습용 대규모 데이터로 바꾸는 경로를 자동화했다는 점이 핵심 메시지다. 국소 근접성만 좇다가 물리적 관통을 허용하는 기존 방법과 달리, 전역 기하 정확도와 비관통을 우선하는 설계 선택이 시뮬레이션 적합성으로 이어진다.

## 한계
논문이 직접 밝힌 한계는 다음과 같다.
- **파운데이션 모델 의존성 상속**: 깊이, 분할, 다시점 생성 모델의 한계를 그대로 물려받는다. 투명하거나 반사가 심한 물체 같은 극단 사례에서는 스케일 드리프트나 국소 추적 오차가 생길 수 있다.
- **정적 카메라 가정**: 다만 논문은 이 가정이 **IOF 자동 검출에만** 적용된다고 선을 긋는다. 마스크 변위로 IOF를 찾는 휴리스틱이 카메라 자체 움직임과 물체 움직임을 구분하지 못하기 때문이다. 핵심 파이프라인(생성·초기화·추적)은 전부 카메라 좌표계에서 돌아가므로 카메라 ego-motion과 수학적으로 무관하고, 유효한 IOF만 주어지면 동적 카메라에서도 성립한다.
- **강체(rigid) 물체에 한정**: 관절형(articulated)이나 변형 가능한(deformable) 범주로의 확장은 향후 과제로 남겼다.
- **장기 시퀀스 드리프트**: 단일 앵커 기반이라 긴 시퀀스에서 누적 드리프트 위험이 있다. VLM을 다중 앵커 검출기로 써서 양방향 전파를 강화하거나, 복원 가능한 구간을 미리 걸러내는 pre-filter로 쓰는 방향을 제안한다.

## 우리 연구와 연결되는 점
- **Hand-Object Interaction**: 정면으로 겹치는 주제다. 특히 $\mathcal{L}_{\text{interact}}$의 설계 — 잡는 동안 손-물체 상대 배치가 강체처럼 유지된다는 가정을 물체 SDF 기반 정점 가중치로 접촉 부위에만 국한시키는 방식 — 는 접촉 모델링 일반에 재사용할 만하다. 또한 "생성한 에셋과 영상 관측 사이의 시각적 유사도가 높으면 DINO 특징 정합만으로 추적이 된다"는 통찰은 템플릿 없는 물체 추적의 대안 경로를 보여준다.
- **Egocentric Vision**: 1인칭 영상은 손 가림이 극심하고 SfM이 잘 깨지는 대표적 조건이라 AGILE의 문제 설정과 직결된다. 다만 이 논문은 3인칭 고정 카메라 벤치마크(HO3D, DexYCB, ARCTIC)로 평가했고 egocentric 벤치마크 결과는 제시하지 않는다. IOF 검출만 정적 카메라를 가정하고 코어 추적은 카메라 움직임과 무관하다는 부록 C의 주장은, 머리 착용 카메라로 옮길 때 IOF 검출을 어떻게 대체하느냐가 핵심 관문이 된다는 뜻이기도 하다.
- **Spatial Audio**: 논문에 오디오 관련 언급은 전혀 없다. 다만 IOF(접촉 개시 프레임)를 마스크 변위 휴리스틱으로 찾는 부분은 오디오로 보강할 여지가 뚜렷하다. 접촉·충격 순간은 오디오 온셋으로 훨씬 뚜렷하게 잡히고, 이는 정적 카메라 가정을 우회하는 대안이 될 수 있다. 나아가 복원된 6D 궤적과 접촉 상태는 물체 위치 기반 공간 오디오 합성의 기하 조건으로도 쓸 수 있다.
