# LUCID: Learning Embodiment-Agnostic Intent Models from Unstructured Human Videos for Scalable Dexterous Robot Skill Acquisition

**arXiv**: 2606.11628 | **주제 분류**: Human-to-Robot Learning | **출판일**: 2026-06-10 | **학회**: 프리프린트
**저자/소속**: Harsh Gupta (University of Illinois Urbana-Champaign), Guanya Shi (Carnegie Mellon University), Wenzhen Yuan (University of Illinois Urbana-Champaign)
**링크**: https://arxiv.org/abs/2606.11628

## 한 줄 요약
로봇 원격조작 데이터를 한 프레임도 쓰지 않고, 인터넷에 널린 사람 조작 영상에서 "다음 1초 동안 장면이 어떻게 변해야 하는가"라는 의도(intent)만 배우고, 그 의도를 실제 관절 명령으로 바꾸는 일은 대규모 병렬 시뮬레이션에서 따로 학습한 뒤 둘을 실시간 폐루프로 붙인 프레임워크다.

## 메인 그림
논문의 Figure 1은 왼쪽에 사람 영상으로 학습하는 의도 모델, 오른쪽에 시뮬레이션으로 학습하는 로봇 제어 정책, 가운데에 이 둘을 결합해 다지 로봇 손(dexterous hand)과 2지 그리퍼(parallel-jaw gripper)에 실제로 배치한 모습을 배치한 개요 그림이다. (arXiv HTML 렌더링이 제공되지 않아 이미지 절대 URL을 확보하지 못해 텍스트로만 서술한다.)

## 선행 연구
사람 영상에서 로봇 조작을 배우려는 흐름은 크게 세 갈래로 나뉜다.

**첫째, 궤적을 뽑아 쓰는 방식(trajectories from video).** 인터넷 영상을 3D 손·물체 궤적으로 복원하거나, 웨어러블·보정된 카메라로 현장에서 찍은 영상을 써서 그 궤적 위에 정책을 학습한다. 문제는 학습된 정책이 녹화된 특정 궤적에 묶이고, 복원 노이즈와 사람-로봇 형태 차이(embodiment gap)를 그대로 떠안는다는 점이다.

**둘째, 영상 생성 모델로 계획을 세우는 방식(plans from video).** 사전학습된 비디오 생성 모델에 초기 관측을 조건으로 넣어 롤아웃 시작 시점에 계획 영상을 한 번 만들고, 별도의 실행기가 그 계획을 따라간다. [NovaFlow](https://arxiv.org/abs/2510.08568) 같은 연구가 여기 속한다. 영상 생성 비용이 워낙 커서 계획은 $t=0$에 한 번만 만들어지고 실행 중에는 갱신되지 않는다.

**셋째, 표현 학습 보조로 쓰는 방식(representations from video).** 사람 영상을 사전학습 자료로 쓰되, 결국 실제 로봇 원격조작 데이터로 공동학습하거나 미세조정해야 하는 계열이다. [DreamDojo](https://arxiv.org/abs/2602.06949) 같은 대규모 world model도 이 범주에 든다. 어떤 변형을 쓰든 로봇 행동 데이터가 병목으로 남는다.

제어 쪽에서는 시뮬레이션 강화학습(sim-to-real RL)이 발전해, 공격적인 도메인 랜덤화 아래 수많은 물체와 궤적을 아우르는 범용(generalist) 감각운동 정책이 나왔다. [SimToolReal](https://arxiv.org/abs/2602.16863), [DextrAH-RGB](https://arxiv.org/abs/2412.01791), [Dex4D](https://arxiv.org/abs/2602.15828) 등이다. 다만 이들이 추론 시점에 따라갈 "기준 궤적(reference)"은 여전히 정책 바깥에서 온다 — 모션캡처, 원격조작, 단일 영상에서 뽑은 포즈, 혹은 메시와 목표 포즈 같은 것들이다.

의도를 표현하는 중간 매개로는 **object-centric flow**(물체 표면 점들의 미래 궤적)가 널리 쓰인다. 메시가 필요 없고 강체·관절형·변형체를 모두 다루며 인터넷 영상에서 바로 뽑을 수 있다. 하지만 flow는 **접촉 순간 손이 어디 있어야 하는지를 말해주지 않는다.** 다지 손처럼 손가락이 여럿인 경우 이 미지정(underspecification)이 치명적이다. 반대로 손 중심 표현(손목 궤적, grasp prior)은 flow가 못 담는 기능적 파지를 담지만 물체 운동을 직접 지정하지 못한다.

## 문제 제기
논문이 겨누는 지점은 세 개다.

**1) 데이터 병목.** 오늘날 주류 로봇 학습 파이프라인은 원격조작 시연이나 구조화된 사람 데이터(모션캡처, 다시점 리그, 웨어러블, 손잡이형 그리퍼)에 기댄다. 전용 수집 장비가 필요하고, 특정 로봇이나 인터페이스에 묶이며, 오직 **작업자 시간에 비례해서만** 규모가 커진다.

**2) 개루프(open-loop) 계획의 취약성.** 비디오 생성 계획은 실행이 계획에서 벗어나는 순간 회복 수단이 없다. 논문의 예시가 선명하다 — 젓기(stirring) 작업에서 첫 파지가 어긋나 숟가락이 튕겨 다른 위치로 날아가면, 낡은(stale) 기준 궤적이 감각운동 정책을 혼란에 빠뜨린다. 게다가 생성 모델은 없던 장면을 환각으로 만들어내기도 한다(예: 닦지도 않은 영역을 닦인 것처럼 그림).

**3) 시뮬레이션의 보상 설계 문제.** 시뮬레이션은 행동 라벨이 붙은 데이터를 무한히 찍어낼 수 있지만 작업마다 보상을 손으로 설계해야 하고, 특히 고수준 의도는 보상으로 옮기기가 몹시 어렵다.

논문의 주장은 이 두 확장 가능한 자원 — 인터넷 영상과 대규모 시뮬레이션 — 이 서로의 약점을 정확히 메운다는 것이다. **영상은 형태에 무관한 의도를, 시뮬레이션은 작업에 무관하고 견고한 감각운동 능력을 제공한다.** 문제는 이 둘을 어떻게 이어붙이느냐다.

## 연구 주제
LUCID는 조작 학습을 **의도(intent, 장면에서 무엇이 바뀌어야 하는가)** 와 **제어(control, 로봇이 그것을 어떻게 달성하는가)** 로 분리하고, 그 사이를 명시적 인터페이스로 잇는다. 핵심 설계는 두 가지다.

- **분리(decoupling)**: 의도 모델 $f_\theta$는 구조화되지 않은 사람 영상으로만 학습하고, 감각운동 정책 $\pi$는 시뮬레이션으로만 학습한다. 원격조작 데이터는 전혀 쓰지 않는다.
- **폐루프(closed-loop) 의도 예측**: 배치 시 $f_\theta$는 한 번의 계획을 뱉는 대신 살아 있는 장면에서 계속 재질의(re-query)된다. 물체 메시도, 모션캡처도, 형태별 적응도 필요 없다.

검증할 주장은 네 개다. (1) 새로운 장면·카메라 시점·물체 인스턴스로 zero-shot 전이되는 의도를 배우는가, (2) 폐루프 예측이 견고성을 높이는가, (3) 같은 의도 모델이 서로 다른 로봇 형태로 확장되는가, (4) 학습 영상 양에 따라 예측 가능하게 좋아지는가.

## 연구 방법

### 전체 구조
두 학습 요소가 **짧은 지평(short-horizon) 기준 궤적** $\mathcal{R}$을 매개로 통신한다. $\mathcal{R}$은 물체 flow와 손바닥(palm) 포즈 두 성분으로 이루어진다.

$$f_{\theta}\big(\mathbf{I}_{t},\ \{\mathbf{x}_{n,0}^{\text{trk}}\}_{n=1}^{N},\ (\mathbf{p}_{0}^{\text{palm}},\mathbf{R}_{0}^{\text{palm}})\big) = \mathcal{R} = \big(\{\mathbf{x}_{n,\tau}^{\text{trk}}\}_{n=1,\tau=1}^{N,T},\ \{(\mathbf{p}_{\tau}^{\text{palm}},\mathbf{R}_{\tau}^{\text{palm}})\}_{\tau=1}^{T}\big)$$

여기서 $\mathbf{I}_t$는 최근 RGB-D 프레임 $F$장을 쌓은 것, $\mathbf{x}_{n,\tau}^{\text{trk}} \in \mathbb{R}^3$은 물체 위 $n$번째 질의점의 3D 위치, $(\mathbf{p}_\tau^{\text{palm}}, \mathbf{R}_\tau^{\text{palm}}) \in SE(3)$은 손바닥 포즈다. 실제 설정은 $F=2$, $N=16$, $T=8$이고 예측 지평은 1초(8 Hz 기준 스텝)다.

### 1) 의도 모델 $f_\theta$ (§3.1)
**아키텍처**: CoTracker3를 "점 토큰 트랜스포머"로 개조했다. 표준 점 추적기와 갈라지는 세 지점이 있다.
- 얼어붙은 DINOv3 ViT-B/16 패치 토큰으로 트랜스포머를 조건화하고, 깊이는 zero-initialized 잔차 어댑터(Conv2d → LayerNorm → Linear)로 같은 토큰 공간에 융합한다.
- 이미 관측된 프레임을 되짚어 추적하는 대신 **$\mathbf{I}_t$에서 미래로 전진 예측**한다.
- $N$개 물체 토큰 옆에 **손바닥 포즈 토큰 하나**를 붙여, 물체 flow와 손 포즈를 함께 뽑는다.

**학습 손실**: 세 스트림에 대한 MSE의 단순 합이다.
$$\mathcal{L}(\theta) = \mathcal{L}_{\text{trk}} + \mathcal{L}_{\text{palm},p} + \mathcal{L}_{\text{palm},r}$$

**사람 픽셀 증강이 핵심 트릭.** 일반적인 기하·광도 증강($p_{\text{aug}}=0.8$) 위에, 사람 마스크 내부 픽셀을 확률 $p_{\text{human}}=0.6$으로 통째로 교란한다. 네 가지 외형 모드를 고정 혼합비로 뽑는다 — 강한 색 왜곡(0.40), 균일 랜덤 RGB 채우기(0.25), 2~6칸 색 패치 격자(0.25), 픽셀별 랜덤 RGB(0.10). 이유가 명확하다. **배치 시 화면에 보이는 손은 사람이 아니라 로봇**이므로, 모델이 시연자 손 외형에 기대지 않고 물체 운동을 추론하게 만들어야 한다.

**감독 신호 추출 파이프라인 (§A.1)**: 작업마다 20k 클립을 Panda-70M, Action100M, Something-Something-V2, EPIC-Kitchens, LVP 메타데이터 릴리스에서 캐낸다(대부분 야생 YouTube 영상). 유일한 수작업 주석은 **분할 프롬프트로 쓸 물체 이름 하나뿐**이다. 클립은 8 Hz로 재샘플링하고 stride 2로 슬라이딩 윈도우를 자른다. 각 윈도우에 네 단계가 돌아간다.

| 단계 | 도구 | 산출물 |
|---|---|---|
| (a) 카메라·깊이 복원 | ViPE | 프레임별 내부 파라미터 $\mathbf{K}_\tau$, 외부 파라미터 $\mathbf{E}_\tau$, metric depth $D_\tau$ |
| (b) 물체·사람 분할 | SAM 3.1 | $M_\tau^{\text{obj}}$, $M_\tau^{\text{hum}}$ (물체 마스크에서 사람 마스크를 빼서 손 픽셀 누출 차단) |
| (c) 물체 flow | DenseTrack3Dv2 | $M_0^{\text{obj}}$ 내부 $N$개 질의 픽셀을 추적 후 $D_\tau$로 역투영 → 3D 궤적 |
| (d) 손 복원 | WiLoR + 강체 정합 | MANO 메시를 깊이에 맞춰 스케일·이동 정렬 → 손바닥 포즈 |

(d)의 강체 정합은 프레임마다 균일 스케일 $s$와 이동 $\mathbf{t}$를 푸는 최소제곱 문제로, $(s, t_x, t_y)$에 대해 선형이고 $t_z$는 닫힌 형태라 프레임당 lstsq 한 번으로 끝난다.

### 2) 범용 감각운동 정책 $\pi$ (§3.2)
Isaac Lab에서 목표 조건부 강화학습으로 학습한다. **$f_\theta$의 출력이나 사람 영상은 학습에 전혀 쓰지 않고, $\mathcal{R}$ 형식의 기준 궤적을 절차적으로(procedurally) 생성한다.**

- **물체**: 박스·구·캡슐·원기둥·평판을 무작위 표면점에 붙여 불리언 합집합으로 만든 약 1k개 형상 풀. 뭉툭한 덩어리형, 길쭉한 도구형, 납작한 책형을 아우른다. 스케일 배수는 $[0.65, 1.1]$에서 뽑는다.
- **기준 궤적**: 네 구간(approach → in-hand motion → goal → disengage)의 사슬이다. 접근 2.5초, 인핸드 0~7.5초, 목표 3.0초, 이탈 2.5초. 한 롤아웃 안에서 파지·인핸드 조작·놓기·해제를 모두 연습시킨다.
- **hand-coupling 모드**가 영리한 장치다. 구간마다 손바닥이 물체를 얼마나 단단히 따라갈지 바꾼다(*full*: 위치·회전 모두 고정, *position-only*: 회전 해제, *none*: 완전 분리). 덕분에 **같은 물체 운동에 대해 여러 유효한 손 전략이 허용**되어, 배치 시 $f_\theta$가 어떤 손 포즈를 주든 정책이 대응할 수 있다.

**행동 공간**: $\mathbf{a}_t = [\mathbf{a}_t^{\text{arm}}; \mathbf{a}_t^{\text{eig}}; \mathbf{a}_t^{\text{hnd}}]$로, 팔 관절 위치 변화량($\mathbb{R}^6$), 아이겐그래스프 계수($\mathbb{R}^5$), 관절별 잔차($\mathbb{R}^{16}$)다. 아이겐그래스프 기저 $\mathbf{E} \in \mathbb{R}^{16\times5}$는 리타게팅된 사람 파지 데이터의 상위 5개 주성분이라, 손가락이 자연스러운 파지 모드를 따라 움직이며 탐색이 안정적 파지 쪽으로 편향된다. 최종 손 변화량은 $\Delta \mathbf{q}_t^{\text{hand}} = \mathbf{E}\mathbf{a}_t^{\text{eig}} + \mathbf{a}_t^{\text{hnd}}$다.

**교사-학생 구조**: 교사 $\pi^T$는 특권 시뮬레이터 상태(물체 전체 표면에서 뽑은 flow, 관절 속도, 물체 포즈)로 PPO 학습한다. 학생 $\pi^S$는 **외부 RGB-D 카메라에서 보이는 표면만으로 제한된 flow + 손목 장착 깊이 이미지**를 받는다. 외부 카메라 위치는 에피소드마다 랜덤화해 $f_\theta$ 학습에 쓰인 사람 영상의 시점 분포를 덮는다.

증류는 PHP를 따라 하이브리드 목적함수를 쓴다.
$$\mathcal{L}_{\text{student}} = \mathcal{L}_{\text{PPO}}(\pi^S) + \lambda_D \|\mu^S - \mu^T\|_2^2$$
$\lambda_D$를 점진적으로 낮춰, 초반에는 모방이 지배하고 격차가 좁혀진 뒤엔 on-policy PPO 항이 넘겨받는다. 순수 모방이 아니어서 **학생이 교사에게 없는 자기만의 입력(손목 카메라)을 적극 활용할 수 있다**는 게 요점이다.

**보상**은 작업 무관한 단일 설계다. (1) 물체 추적(주 신호) — 현재 질의점을 lookahead 목표에 붙이고 목표 포즈 도달 시 보너스, (2) 손바닥 포즈 추종과 손가락 접촉 — 형태별 편차를 허용하는 shaping 보상, (3) 정규화 — 큰 행동과 위험한 관절 자세에 페널티.

**커리큘럼(ADR)**: 단일 스칼라 $\rho \in [0,1]$이 여러 손잡이를 동시에 조인다. 중력은 $0.0981\ \text{m/s}^2$에서 $9.81\ \text{m/s}^2$로, 궤적 성공 허용오차는 $(0.07\text{m}, 0.7\text{rad})$에서 $(0.04\text{m}, 0.4\text{rad})$로, 물체 외란 렌치는 0에서 $\pm 0.15\text{N}, \pm 0.01\text{Nm}$로 이동한다.

### 3) 실제 배치 (§3.3)
느린 의도 주기(intent cycle)가 빠른 제어 루프를 감싸는 구조다. 배치 스택 전체가 **RTX 5090 한 장**에서 돈다.

| 단계 | 주기 (Hz) |
|---|---|
| SAM 3.1 마스크 갱신 | 1 |
| 의도 모델 $f_\theta$ | 1 |
| DenseTrack3Dv2 슬라이딩 윈도우 추적 | 30 |
| 감각운동 정책 $\pi^S$ | 50 |

의도 주기 사이에는 3D 점 추적기가 현재 질의점을 실시간 장면과 일관되게 유지하고, lookahead 창이 $\mathcal{R}$의 1초 flow 지평 위를 전진한다. 끝에 도달하면 $f_\theta$를 재질의한다. $f_\theta$는 사람 영상으로 학습했으므로 출력이 외부 카메라 좌표계로 나오고, 이를 로봇 베이스 좌표계로 변환해 쓴다. 손목 깊이는 센서 내장 스트림이 근거리에서 너무 노이즈가 심해, IR 페어에 Fast-FoundationStereo(추론 5 ms)를 돌린 결과를 쓴다.

## 실험 결과 / 연구 의의

### 웹 영상 지도 작업 (§4.1)
젓기(stirring), 닦기(wiping), 담기(binning) 세 작업을 각각 20k 사람 영상 클립으로 지도했다. 각 작업을 세 시나리오에서 10회씩 평가하며, 시나리오는 **물체 인스턴스·테이블 배치·외부 카메라 포즈를 동시에 바꾼** 의도 모델 기준 out-of-distribution 조건이다.

| 방법 | 웹 지도 3작업 평균 성공률 |
|---|---|
| 개루프 비디오 생성 계획기 (Veo 3.1) | 28% |
| **LUCID (폐루프, 다지 손)** | **73%** |

같은 감각운동 정책에 계획만 개루프로 바꿔 넣은 비교라, 격차가 순수하게 **폐루프 의도**의 기여로 해석된다. 개루프 계획은 2~3분에 하나씩 생성되고, 명백히 엉뚱한 생성은 걸러내고 재샘플링까지 해준 조건인데도 이 결과다.

부수적으로 눈여겨볼 관찰이 둘 있다.
- **변형체 일반화**: 시뮬레이션에서는 강체만 학습했는데, 실제로는 휴지·수건·천 같은 변형 물체를 다룬다.
- **작업 창발적 합성(emergent composition)**: 케첩을 휴지로 닦은 뒤 작업 공간 근처에 통을 놓아주자, 의도 모델이 더러워진 휴지를 집어 통에 넣었다. **binning 감독을 전혀 주지 않았는데도** 그렇다. 저자들은 20k 규모 wiping 클립 풀 안에 우연히 섞여 있던 담기 행동 덕으로 본다.

실패는 두 부류로 뭉친다. 손이 숟가락을 덮어버리는 식의 **가림에 의한 지각 실패**, 그리고 테니스공이 테이블에서 굴러떨어지는 식의 **회복 불가 상태**다.

### 의도 데이터 스케일링 (§4.2)
binning 학습 코퍼스를 $\{1k, 2k, 5k, 10k, 20k\}$ 클립으로 스윕하고, 각 지점마다 세 시나리오 × 10회 = 30회 실제 시행과 1k 클립 held-out 의도 손실을 함께 쟀다. 손실은 코퍼스 크기에 따라 꾸준히 떨어지고 실제 성공률도 함께 오른다.

질적 변화가 흥미롭다. 1k~2k 구간에서는 정책이 물체까지는 가지만 **통을 식별하지 못한다.** 5k~10k 구간에서 통 위치 파악이 창발하지만 **놓는 정렬이 여전히 약해** 놓는 순간 통을 빗나가는 일이 잦다.

다만 논문은 스케일링 주장에 스스로 브레이크를 건다. 고정 지수 멱법칙 $L(M) = c + aM^{-\alpha}$을 $\alpha \in \{0.05, ..., 0.25\}$로 적합해보면 관측 구간(1K~20K)에서는 후보들이 거의 구분되지 않고, 1000배 외삽하면 서로 크게 벌어진다. 즉 **"영상이 많을수록 좋아진다"는 추세는 지지되지만 장기 예측치는 확정되지 않는다.**

### 로봇 형태 전이 (§4.3)
push-T(T자 블록을 목표 자세로 밀기)와 cable routing(케이블을 두 고정구에 꿰기) 두 작업을 각각 **자체 촬영 스마트폰 영상 1시간**(거치된 iPhone 16 Pro Max)으로 지도했다. 같은 의도 예측을 두 감각운동 정책 — LEAP 다지 손과 2지 그리퍼 변형 — 에 그대로 흘려보내고 형태별·작업별 15회씩 시행했다.

| 형태 | 두 작업 합산 성공 |
|---|---|
| LUCID (LEAP 다지 손) | 19/30 (63%) |
| LUCID (2지 그리퍼) | 19/30 (63%) |

형태가 판이한데 총 성공률이 같다. 세부는 갈린다 — **cable routing은 오히려 그리퍼가 유리**하다. 마주보는 두 턱이 가느다란 케이블을 잡기에 알맞은 반면 다지 손은 정밀 파지에 애를 먹는다. 그리퍼 정책은 동일한 절차적 궤적·물체·보상으로 학습하되 손가락 조작 보상만 제거하고, 접촉 계수를 $c(\mathbf{f}) = 2s_{\text{jaw1}} + 2s_{\text{jaw2}}$로 축약했다. 사람 손바닥으로 지도된 palm 포즈 기준은 **그리퍼의 닫힘 축을 사람 손의 네 손가락-엄지 대향 방향에 맞추는** 좌표계 정렬로 옮긴다.

두 형태 모두 실패는 의도 모델의 예측이 정책을 앞으로 못 미는 out-of-distribution 상태에 몰리는데, 저자들은 1시간짜리 작은 코퍼스 탓으로 본다.

### 감각운동 정책 ablation (§4.4)
시뮬레이션에서 시드 3개로 네 가지를 뺐다. 논문은 에피소드 보상 곡선(Fig. 7)으로 제시하고 수치 표는 없다.

| 제거 항목 | 관찰된 결과 |
|---|---|
| 토큰화 인코더 → MLP 연결 | 물체 위치는 잡지만 안정적 파지 접촉과 인핸드 조작에 필요한 **점별 세부 정보 상실** |
| 아이겐그래스프 기저 제거 | 탐색 공간이 훨씬 커짐. 기저가 있으면 초반엔 아이겐 모드로 자연스러운 파지를, 후반엔 개별 관절로 미세 조작을 익힘 |
| 하이브리드 증류 → DAgger-BC | 경쟁력은 있으나 교사를 모방만 하므로 **학생이 자기 입력(손목 카메라)을 온전히 활용 못 함** |
| 손목 카메라 제거 | 물체 기하가 미해결로 남아 MLP 인코더 제거와 비슷한 양상 |

질의점 개수 $N$은 늘릴수록 보상이 오르지만 수확 체감이라 $N=16$을 운용점으로 골랐다.

**의의**: 두 개의 독립적으로 확장 가능한 감독 자원 — 인터넷 영상과 대규모 시뮬레이션 — 을 짝지어, **원격조작 없이도 규모가 커지는** 다지 조작 학습 경로를 실증했다는 것이 핵심 메시지다. 특히 flow 단독(물체가 어떻게 움직일지)도 손 포즈 단독(손이 어디 있을지)도 아닌 **둘의 결합**을 형태 무관 인터페이스로 삼았다는 점, 그리고 그 인터페이스가 다지 손과 2지 그리퍼를 동시에 구동한다는 점이 설계의 요체다.

## 한계
논문이 §5에서 직접 밝힌 세 가지다.

- **파이프라인 취약성.** 시스템이 지나치게 모듈화되어 있다. SAM 3.1, DenseTrack3Dv2, ViPE, WiLoR 각각이 데이터 추출 시점과 배치 시점 양쪽에서 실패 지점을 하나씩 추가한다. 인핸드 가림이나 무늬 없는 물체 같은 지각 오류가 사슬을 타고 감각운동 정책까지 전파된다. 원본 영상에서 의도 모델을 end-to-end로 학습하면 이 사슬을 단일 학습 표현으로 대체할 수 있다고 본다.
- **작업 종료 조건의 부재.** 검증 가능한 종료 조건이 없는 작업에서는 의도 모델이 무한 루프에 빠진다. **젓기는 현재 외부에서 강제로 멈춰줘야 한다.** 또 새 작업 학습마다 코퍼스를 손으로 걸러야 해서 확장성이 나쁘다. 텍스트 조건화로 수천 개 작업까지 넓히면 두 문제를 함께 풀 수 있다고 제안한다 — 고수준 계획기가 "젓기" → "숟가락 내려놓기" 식으로 하위 작업을 이어붙이는 그림이다.
- **손실이 있는 명시적 인터페이스.** 3D flow + 손바닥 포즈 인터페이스는 손으로 설계한 것이라 손가락 형상이나 미세 접촉 같은 정보를 버린다. 의도를 정확히 예측하더라도 사람 시연을 온전히 재현하는 데 필요한 단서가 빠진다. 두 하위 모델에 걸쳐 함께 최적화되는 잠재(latent) 중간 표현이면 인터페이스가 정책이 필요로 하는 신호에 맞춰 적응할 수 있다.

여기에 논문이 명시적으로 한계로 적지는 않았지만 읽어둘 만한 점 — 의도 모델은 **작업마다 따로 학습**하며(§A.2.2), 웹 지도 3작업 각각에 20k 클립이 필요했다.

## 우리 연구와 연결되는 점

- **Human-to-Robot Learning**: 정면으로 겹친다. 가장 재사용 가치가 높은 통찰은 **"사람 픽셀을 공격적으로 파괴하는 증강"** 이다. 사람-로봇 외형 격차를 도메인 적응이나 리타게팅으로 푸는 대신, 학습 단계에서 손 외형 정보를 아예 못 쓰게 만들어 물체 운동 추론만 남기는 접근은 구현이 단순하고 다른 human-to-robot 파이프라인에 그대로 이식할 수 있다. 또 하나는 **hand-coupling 모드**다. 시뮬레이션 기준 궤적을 만들 때 "같은 물체 운동에 여러 손 전략이 유효하다"는 다대일 관계를 명시적으로 주입해두면, 배치 시 의도 모델이 뱉는 손 포즈가 정확하지 않아도 정책이 무너지지 않는다. 사람과 로봇의 형태 차이를 흡수하는 값싼 장치다.

- **Hand-Object Interaction**: 의도 표현을 **물체 flow + 손바닥 포즈**로 쪼갠 선택이 직접적으로 유관하다. 논문의 진단대로 flow는 접촉 시점 손 위치를 지정하지 못하고, 손 중심 표현은 물체 운동을 지정하지 못한다. 다만 LUCID가 택한 절충 — 손바닥 6-DoF 하나만 남기고 손가락 관절은 전부 버림 — 은 저자들 스스로 "손실이 있다"고 인정한 지점이다. 손가락 수준 접촉을 어떻게 형태 무관하게 표현할 것인가는 열려 있다. 감독 신호 추출 쪽에서는 WiLoR MANO 메시를 ViPE 깊이에 프레임별 강체 정합으로 붙이는 절차(닫힌 형태 lstsq 한 번)가 가볍고 실용적이다.

- **Egocentric Vision**: 학습 코퍼스에 EPIC-Kitchens와 Something-Something-V2가 포함되어 1인칭·준1인칭 영상이 감독 신호로 쓰인다. 다만 **배치는 고정 외부 RGB-D 카메라 + 손목 깊이 카메라** 조합이고, $f_\theta$의 출력을 외부 카메라 좌표계에서 로봇 베이스 좌표계로 변환하는 구조다. 즉 학습은 자유롭게 움직이는 카메라의 영상을 쓰지만 추론은 고정 시점을 전제한다. 이 비대칭을 ViPE의 카메라 포즈 복원과 에피소드별 카메라 랜덤화로 메우는 방식이, 1인칭 배치로 옮길 때 무엇을 더 해야 하는지 가늠하는 참고점이 된다.

- **Spatial Audio**: 논문에 오디오 관련 언급은 전혀 없다. 다만 논문이 스스로 꼽은 두 한계가 오디오와 맞물릴 여지가 있다. 첫째, **검증 가능한 종료 조건의 부재**(젓기가 외부 정지 신호를 필요로 함)는 접촉·충돌 소리로 사건 경계를 잡는 문제로 옮겨볼 수 있다. 둘째, **인핸드 가림으로 인한 지각 실패**는 시야가 막힌 상황에서도 접촉 상태를 알려주는 비시각 신호가 보완할 수 있는 전형적 사례다. 어느 쪽도 논문이 다루지 않은 확장 방향이다.
